<UserControl x:Class="ServerMessaging.SampleApplication.Views.Controls.Terminal"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <DockPanel LastChildFill="True">
        <TextBox Style="{StaticResource DarkTheme}"
                 Text="{Binding Promt, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}"
                 DockPanel.Dock="Bottom"/>
        <TextBlock VerticalAlignment="Stretch"
                   HorizontalAlignment="Stretch"
                   TextWrapping="Wrap"
                   Style="{StaticResource DarkThemeBlock}"
                   Text="{Binding Text, UpdateSourceTrigger=PropertyChanged}"/>
    </DockPanel>
</UserControl>
