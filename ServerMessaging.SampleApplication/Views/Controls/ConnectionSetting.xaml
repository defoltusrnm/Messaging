<UserControl x:Class="ServerMessaging.SampleApplication.Views.Controls.ConnectionSetting"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <StackPanel Margin="5">
        <StackPanel Orientation="Horizontal"
                    Margin="5">
            <TextBlock Text="Address"/>
            <TextBox Text="{Binding Address, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}"/>
        </StackPanel>
        <StackPanel Orientation="Horizontal"
                    Margin="5">
            <TextBlock Text="Port"/>
            <TextBox Text="{Binding Port, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}"/>
        </StackPanel>
        <StackPanel Orientation="Horizontal"
                    Margin="5">
            <TextBlock Text="Use as Server"/>
            <CheckBox IsChecked="{Binding IsServer, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}"/>
        </StackPanel>
        <Button Margin="5"
                HorizontalAlignment="Left"
                VerticalAlignment="Center"
                Padding="5">
            <TextBlock Text="Connect"/>
        </Button>
    </StackPanel>
</UserControl>
